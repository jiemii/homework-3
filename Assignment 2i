
function setup() { 
  createCanvas(400, 400);
} 
for (var i = 20; i <= 480; i = i + 10) {
  var startX = i;
  var startY = 480;
  var endX = i;
  var endY = 20;
  line(startX, startY, endX, endY);
}
